# 取扱説明書 更新依頼（JIS C 0617 図記号の精査・2026-08-06 分）

codex への依頼: 以下を `docs/manual.md`（および PDF 版・アプリ内ヘルプ画像）へ反映してください。
実装は `index.html` に反映済み・全125テストパス済みです。アプリ内ヘルプ「シンボルデザイン（JIS/IEC切替）」の
文言も更新済みなので参考にできます。

参照した規格データベース: <https://pumpkinpai.web.fc2.com/Supple/Database/Documents_711.html>
（JIS C 0617 全8部の図記号番号・名称・図形。今回は各図記号の画像と実装を1点ずつ突き合わせました）

---

## A. 今回の変更点（マニュアル修正が必要）

### A-1. 変圧器に JIS C 0617 06-09-02（巻線形）を追加、06-09-01（円形）を規格の形へ是正 ★重要

変圧器のシンボルデザインが **3種 → 5種** になりました。

| 値 | 表示名 | 説明 |
|---|---|---|
| `standard` | 巻線バー＋タップ（設備標準） | 変更なし。見本PDF実測形 |
| `circles` | 2巻き変圧器・円形（JIS 06-09-01） | **形状是正**。2つの円を規格どおり深く交差させた（中心間4.7mm・半径3.4mm。従来は重なりが浅く2つの円が接するだけに見えていた） |
| `circlesCore` | 2巻き変圧器・円形＋磁心（JIS 06-09-01） | **形状是正**。磁心線を**2本→1本の実線**へ（JIS C 0617 の磁心は1本線。2本線は旧JIS表記） |
| `coils` | 2巻き変圧器・巻線形（JIS 06-09-02） | **新規**。巻線を半円コブ4個のコイル記号で表し、上下の巻線を凸側が向かい合う向きに置く。磁心線なし |
| `coilsCore` | 2巻き変圧器・巻線形＋磁心（JIS 06-09-02） | **新規**。上記＋巻線間に磁心の直線1本 |

- 06-09-01 と 06-09-02 は**どちらも「2巻き変圧器（一般用図記号）」**で、規格上は同格の2様式です
  （円形＝巻線を円で表す様式、巻線形＝巻線をコイル記号で表す様式）。マニュアルでもそう説明してください。
- **寸法・接続点は5種すべて共通**（左右15mm、上下は設備行ピッチ9.5mm、上タップは主ピンの1行上）。
  切替しても配線はずれません。
- 円形2種の**上タップ線が円に届かず0.9mm浮いていた不具合も修正**しました（タップ線を円周まで延長）。
- 巻線形の上タップは、巻線端と同一節点に見えないよう**コブ間の谷（X=6.25/8.75mm）へ水平スタブで引き込む**
  引き出し方にしています（巻線バー形と同じ考え方）。

**修正箇所**

- `docs/manual.md` 1966行目付近の表:
  `| 変圧器 | 巻線バー＋タップ（standard） / 二巻線円形JIS（circles） / 二巻線円形＋鉄心JIS（circlesCore） |`
  → 上表の5種に差し替え。
- 1854行目付近「変圧器: 縦の巻線バー2本+タップ端子円…」の説明に、JIS 06-09-01/06-09-02 の2様式が
  選べる旨を追記。

### A-2. リアクトルの磁心線を1本へ是正、旧JIS 2本線を別デザインへ分離

| 値 | 表示名 | 説明 |
|---|---|---|
| `standard` | コイル（JIS 04-03-01・コブ3個） | 変更なし |
| `core` | 磁心入り（JIS 04-03-03・磁心線1本） | **形状是正**。従来の2本線から1本の実線へ |
| `coreOld` | 鉄心入り（旧JIS・2本線） | **新規**。従来の見た目を残したい図面用 |
| `box` | 箱形（IEC） | 変更なし |

- 既存図面で `core` を使っていた箇所は**見た目が1本線に変わります**。従来の2本線に戻すには `coreOld` を選択、
  という案内を入れてください。
- **修正箇所**: 1967行目付近の表、および 436行目「リアクトル（山形コブ3個）」の記述。

### A-3. 接地デザインの名称是正と「機能接地」の追加 ★名称の誤り

円で囲む接地は規格上 **「保護接地」(JIS C 0617-2, 02-15-03)** です。従来「清浄接地」と表示していたのは誤りでした。

| 値 | 旧表示名 | 新表示名 |
|---|---|---|
| `standard` | 斜線ハッチ（設備標準） | 変更なし |
| `bars` | 横棒3本 | **接地・一般（JIS 02-15-01・横棒3本）** |
| `circle` | **清浄接地（円囲み）** ← 誤り | **保護接地（JIS 02-15-03・円囲み）** |
| `functional` | （なし） | **機能接地（JIS 02-15-06・弧囲み）** ← 新規 |
| `chassis` | シャーシ接地（櫛形） | 変更なし |

- 機能接地は保護接地と違い、囲みが**閉じた円ではなく下向きの弧（お椀）**です。この違いを本文で説明してください。
- 内部値 `circle` は互換のため据え置きです（既存図面はそのまま読めます）。
- **修正箇所**: 1958行目付近の表。`清浄接地` の語は本文からも消してください。

### A-4. 負荷開閉器（JIS 07-13-08）の限定図記号を是正 ★形状の誤り

負荷開閉装置の限定図記号（07-01-04）は **小さい「円」** です。従来は**半円**で描き、さらに断路機能の
止め棒を残していたため、規格の形と違っていました。

- 修正後: 固定接点を小円（半径0.9mm）で描き、**断路の止め棒は付けない**（止め棒があるのは断路器 07-13-06 のほう）。
- 寸法・接続点は断路器と共通のままです。
- **修正箇所**: 1983行目付近の表の説明文と、負荷開閉器の形状に触れている箇所。

### A-5. 集計値の更新

- アプリ内ヘルプでは「接地4種→**接地5種**」「変圧器3種→**変圧器5種**」「リアクトル3種→**リアクトル4種**」へ更新済みです。
- `docs/manual.md` 1879行目「搭載デザインは合計100種類以上です」は据え置きで問題ありません
  （今回の純増は4様式）。数値を具体的に書いている箇所があれば +4 してください。

---

## B. 精査して「正しかった」もの（変更不要・マニュアルの記述もそのままでよい）

codex が誤って直さないよう、確認済みの項目を残します。

- **限時接点 07-05-01〜04**: ∩＝オンディレー（限時動作）、∪＝オフディレー（限時復帰）の割当は規格どおりでした。
  規格の 07-05-01（メーク・限時閉路）と 07-05-03（ブレーク・限時開路）が同じ限定記号の向き＝オンディレー群、
  07-05-02/07-05-04 がオフディレー群であることと一致します。a接点/b接点×オン/オフの4様式もそろっています。
- **ブザー系 08-10-06 / 08-10-10 / 08-10-09**: 08-10-06＝上ふくらみの半円（弦が下）、08-10-10＝上下反転（弦が上）、
  08-10-09＝頂点が上の三角形。端子線を弦の内側から下ろす描き方も規格どおりでした。
- **操作機構の限定図記号 02-13-01/03/04/05/08/10/23**: 番号と形（手動＝T字／引き＝⊐／回転＝クランク／
  押し＝⊏＋軸／非常停止＝キノコ半円／足踏み＝斜め踏板／電磁＝箱）はいずれも一致。
- **位置スイッチ 07-08-01 / 07-08-02**、**遮断器 07-13-05（×＋開き線）**、**断路器 07-13-06（止め棒＋開き線）**、
  **接触器主接点 07-13-02（半円の開口が可動接点側）**、**ブリッジ整流 06-14-04**、**接地一般 02-15-01** も一致。

---

## C. 参照ページにあって未実装の図記号（今後の追加候補・実装はしていません）

マニュアルに「対応予定/未対応」として書く必要はありませんが、要望が出たときの一覧として残します。
いずれも既存部品と**接続ピン数が違う**ため、追加には新しい部品種別が必要です。

| 番号 | 名称 | 備考 |
|---|---|---|
| 06-09-04 / 06-09-05 | 3巻き変圧器（円形／巻線形） | 6ピン構成では表現できない |
| 06-09-06 / 06-09-07 | 単巻き変圧器（円形／巻線形） | 3端子 |
| 06-13-01 | 計器用変圧器（VT/PT） | 形は 06-09-01 と同じ |
| 06-14-03 | 整流器（順変換装置） | 箱＋対角線＋∿/＝ |
| 06-02-05 / 06-02-07 | 三相巻線 三角結線／星形結線 | 結線記号 |
| 07-02-04 / 07-02-05 | 非オーバーラップ切替スイッチ／オフ位置付き切替接点 | |
| 07-11-05 | 多段スイッチ（最大4位置） | |
| 07-15-22 | 電気式継電器で構成される作動装置 | |
| 07-19-01 / 07-20-02 | 近接センサ／近接検出装置 | |
| 08-01-02 / 08-06-01 | 記録計／熱電対 | |
| 02-16-01 / 02-16-02 | 理想電流源／理想電圧源 | |

---

## D. スクリーンショット再取得

`.codex_tmp/capture-manual-screenshots.js` を参考に、以下を再取得してください。

1. **変圧器5種**（`standard` / `circles` / `circlesCore` / `coils` / `coilsCore`）を並べた見本
   → `docs/images/manual/32-symbol-variants.png` に含まれる「円形+鉄心変圧器」は形が変わったので**要差し替え**。
2. **接地5種**（斜線ハッチ／一般／保護接地／機能接地／シャーシ）を並べた見本。
3. **リアクトル4種**（コブ／磁心1本／旧JIS2本／箱形）。
4. **断路器と負荷開閉器の並び**（止め棒の有無・小円の違いが分かるように）。
5. 21.3「シンボル様式」節に変圧器の作図例があれば、上タップ線の浮きが直った状態で再取得。

---

## E. 検証状況

- `npm run check`（インラインscript構文チェック）: パス
- `npx playwright test`: **125件すべてパス**（新デザイン4種を含む全バリアントの配置・選択テストを含む）
- 接続ピン（アンカー）は変更していないため、既存図面の配線・接続点への影響はありません。
  スキーマバージョンの更新も不要です（マイグレーション不要）。
