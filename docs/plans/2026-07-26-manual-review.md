# 取扱説明書の精査結果（2026-07-26）

対象: `docs/manual.md`（1250行超・28章）と `docs/PDF/manual.pdf`（209ページ）
基準: コミット `61f2d57`（Update manual and add editable wire bends）時点の `index.html`

**この文書は指摘のみで、取扱説明書・PDF・コードは一切変更していません。**

---

## 1. 精査の方法

実装（`index.html`）を正として、実ブラウザ上で機械的に突き合わせた。

| 確認手段 | 内容 |
|---|---|
| 実ブラウザでのUI照合 | ボタン名・パネル項目・メニュー項目・ミニツールバー・右クリックメニューの実文言を取得して本文と比較 |
| 既定値の実測 | 全78種別の既定寸法・接続ピン間隔を `defaultElement` / `elementConnectionAnchors` から取得 |
| テンプレート生成 | 設備ラダー・汎用ラダー・PLC入出力・インターロック・ラング回路を実生成し、§21.5の基準座標と比較 |
| 定数の照合 | 履歴上限・世代数・世代間隔・合体猶予・接続点半径・グリッド・ズーム範囲・CSV分割点数・schemaVersion |
| 図の整合 | 図参照58件のファイル存在、未使用画像、図番号の重複、本文からの図番号参照 |

PDFは `manual.md` をブラウザ印刷した成果物（PDF作成日時 2026-07-25 19:35 UTC ＝ コミットの約5分前、画像116点埋め込み）で、**md と同一内容**である。PDFの文字はサブセットフォントのCID符号化のため直接の文字列検索はできないため、以下の指摘はすべて生成元の `manual.md` の行番号で示す（**PDFにも同じ誤りがそのまま出ている**）。

---

## 2. 誤りの指摘

### 2-1.【要修正】ミニツールバーの配線操作名が旧名称のまま（2か所）

| 箇所 | 記載 | 実際 |
|---|---|---|
| `manual.md:477` | 「配線では線番・**曲げ**・分割・削除など」 | 「線番・**曲がり角＋**・分割・削除」 |
| `manual.md:2149` | 「配線開始、回転、複製、編集、線番、**曲げ**、分割、削除」 | 同上 |

同じコミットでミニツールバーの項目を `["曲げ", flipSelectedWireBend]` → `["曲がり角＋", addSelectedWirePoint]` へ変更しているが、本文が追従していない。**名称だけでなく機能も別物**（旧＝折れ向き反転、新＝曲がり角の追加）なので、読者が「曲げ」を探しても見つからない。

実測（配線選択時のミニツールバー）: `["線番","曲がり角＋","分割","削除"]`

### 2-2.【要修正】右パネルのボタン名が旧名称のまま（章内で不統一）

| 箇所 | 記載 | 実際のボタン名 |
|---|---|---|
| `manual.md:800` | 「**「中間点追加」**を押すと…**「中間点削除」**を押すと最後の中間点を削除します」 | 「**曲がり角追加**」「**最後の曲がり角削除**」 |
| `manual.md:807` | 「右パネルには**「中間点追加」**「直交整形」…が表示されます」 | 同上 |

同じ §9.1 の前半（`manual.md:747-751`）では新名称「曲がり角追加」「曲がり角＋」「最後の曲がり角削除」で説明しており、**同一章内で新旧の名称が混在**している。追記部分だけを直して既存段落を直し忘れたものと見られる。

実測（配線選択時の右パネル）: `曲がり角追加 / 最後の曲がり角削除 / 直交整形 / 折れ向き反転 / 終点を延長 / 終点をトリム / 配線を分割 / 中央ギャップ追加 / 交差ギャップ追加 / 全解除`

なお `manual.md:800` の後半「2点のL字配線では、いま表示されている折れ角（折れ向き反転を含む）がそのまま実際の点になります」と、`manual.md:802` の「配線を分割はL字なら折れ角の位置で2本に分かれる」は**現在の実装と一致している**（実測: `[[40,40],[120,90]]` orthoFlip=false → `[[40,40],[40,90]]` と `[[40,90],[120,90]]`)。修正が必要なのはボタン名だけ。

### 2-3.【要修正】ブザーの寸法が本文内で矛盾（幅15mmは誤り）

| 箇所 | 記載 |
|---|---|
| `manual.md:1975`（§21.5 接続ピンの正規化 表） | 「**ブザー、ファン** ｜ 幅**15mm**」 |
| `manual.md:1916`（§21.4） | 「ブザーの新規既定寸法は**12×6mm**で」 |

実測: ブザーの既定寸法は **12×6mm・左右接続ピン間隔12mm**、ファンは 15×15mm・ピン間隔15mm。
→ 表の行は「ブザー 幅12mm ／ ファン 幅15mm」のように**分けて書くのが正**。ファン側は正しい。

（2026-07-24 のコミット「Shrink the buzzer symbol to match other parts」で 15×9 → 12×6 へ縮小した際の追従漏れ）

### 2-4.【要修正】図番号「図9-1」が重複

| 箇所 | 図題 |
|---|---|
| `manual.md:753` | 「**図9-1** 選択済み配線の曲がり角編集」（今回追加された図） |
| `manual.md:766` | 「**図9-1** 同じ始点・終点で比較した4つの配線形状」（従来からある図） |

図9-2（`manual.md:810` 配線の点編集）は別に存在するため、単純な繰り下げはできない。番号の振り直しが必要。
図参照58件のうち番号重複はこの1件のみで、**画像ファイルの欠落・未使用画像・本文からの未定義図番号への参照は0件**だった。

### 2-5.【要修正・軽微】§3.1 上部ツールバーの一覧に実在ボタンが抜けている

`manual.md:77-87` の一覧は 新規／開く／復元／保存／検索／ヘルプ／表題欄／PDF印刷／A3面付／SVG／PNG の11項目だが、実際のツールバーには次も並んでいる。

- **プレビュー**（`printPreviewBtn`。PDF印刷の左隣） — §17.0 では詳しく説明されているのに、§3.1 の一覧にだけ無い
- **U（元に戻す）／R（やり直し）／X（削除）** のクイックボタン
- **S／W／T**（選択・配線・文字のツール切替ボタン）

実測のツールバー並び: `新規 / 開く / 復元 / 保存 / 検索 / ヘルプ / 表題欄 / プレビュー / PDF印刷 / A3面付 / SVG / PNG / S / W / T / U / R / X`

### 2-6.【要確認・軽微】パレットの表示名と本文の名称の不一致

`manual.md:431` は「ケーブルマーカー」だが、パレットのタイル表示は「**ケーブル**」（内部種別 `cableMarker`）。
§24.1・§24.17 でも「ケーブルマーカー」と呼んでいるため、パレット表示名を併記するか、どちらかへ統一するのが望ましい。

---

## 3. 正しいことを確認した主な記述（再確認不要）

誤りが無いことを実測で確認した範囲。数値・名称はすべて実装と一致していた。

**寸法・接続ピン（§21.5 表）**
- a/b接点10mm・遮断器12.5mm・接点ブロック15mm・変圧器15mm・光電/マット/リード15mm（既定15×8.4mm）
- ソレノイド弁25mm・センサ25mm・電源35mm・ファン15mm
- 機器箱30×25mm・端子箱22.5×15mm・モータ接地ピン第1ピンから12.5mm
- 端子箱の引き出し線6mm、箱内文字サイズ既定2.2mm（`boxLabelFontSize || 2.2`）

**テンプレートの基準位置（§21.5 表）** — 実生成して実測
- 設備ラダー: 左レール32.5／右レール147.5／母線端30・217.5
- 汎用ラダー: 左右母線35／175
- ラング回路: 接点 `45 + 17.5×k`（実測 45, 62.5）、終端コイル135
- PLC入力回路: 行ピッチ10・先頭行のピンY=37.5
- インターロック: 境界端子中心102.5

**定数・仕様**
- 履歴タイムライン最大60件（`HISTORY_LIMIT`）、同一項目の連続変更を4秒以内で1件に合体
- 自動バックアップ 約5分間隔・3世代（`BACKUP_GEN_INTERVAL_MS`/`BACKUP_GEN_COUNT`）
- 接続点の黒丸半径0.5mm、グリッド既定2.5mm、ズーム0.8〜24倍・1クリック約1.25倍
- 端子CSV／PLC CSV は17点ごとにページ分割（`perPage = 17`）
- 現在の保存形式 schemaVersion 4、新規ページの既定図枠は `compact`＝「基本: 右下表題欄」、既定用紙A4縦

**操作・UI**
- 同じ要素を0.45秒以内に2回押すその場編集、2回目からドラッグすると移動が優先（§7.1）
- 複製・貼付は5mmずらし（§7.6）、ドラッグ移動で第1接続ピンをグリッドへ乗せ直し・接続点を最優先で吸着（§7.7）
- 左右ミラーは記号・DXF・接続点を同時反転／再実行で復帰／対称図形は変更せず通知（§24.18）
- プロジェクト一括更新は履歴1件・Ctrl+Z 1回で復帰（§24.10）
- 配線ドラッグ中のキー操作 `1`〜`4`／`Tab`／`R`／`A`／`N`／`J`（§24.9・§24.18）はすべて実装済み
- 連続配置の既定はON（`continuousPlacement = true`）（§24.9）
- 複数選択時のボタン「回路として登録」は実在（§15.1）、右パネルの中間点欄は「中間1 X」「中間1 Y（上が＋）」（§9.1）
- 等間隔複製の既定は複製数3・間隔X 0・間隔Y 9.5mm（§24.18）
- 監査の検査ルール8種＝未採番配線／ネット整合／空ラベル／用紙外／未接続端点／コイル・接点／信号矢印の相手／盤配置（§12.7）
- 信号矢印の種別＝参照／発信元／受信先（§9.7）、下絵メニュー3項目（§4.6）
- 図枠6種の名称と並び（§5.3）、角ハンドルは固定形状シンボル・接続点・ロック要素に出ない（§7.1）
- パレット78種別と §6.2 の列挙はほぼ一致（差分は 2-6 の表示名のみ）

---

## 4. 参考: コード側で気づいた点（取扱説明書とは別件）

- テスト（`npx playwright test`）は **114件中114件合格**。1回目の実行で `文字・端子名の連続入力中にフォーカスを維持する` が `beforeEach` の初期化（`page.goto`）で1件失敗したが、単体実行・再実行では合格したため**環境要因のフレーク**と判断した。コミット `61f2d57` の変更による回帰は確認されていない。
- 今回追加されたテスト「選択済み配線へ曲がり角を追加しハンドルと座標で編集できる」は正常に動作している。

---

## 5. 修正時の推奨手順（codex向け）

1. `manual.md:477` と `manual.md:2149` の「曲げ」→「曲がり角＋」（機能の説明も追加が要る）。
2. `manual.md:800` と `manual.md:807` の「中間点追加／中間点削除」→「曲がり角追加／最後の曲がり角削除」。§9.1前半と用語を統一。
3. `manual.md:1975` の表を「ブザー 幅12mm」「ファン 幅15mm」へ分割。
4. `manual.md:766` 以降の図番号を振り直し（`図9-1` の重複解消。以降の図番号との整合に注意）。
5. `manual.md:77-87` のツールバー一覧へ「プレビュー」ほかを追記。
6. `manual.md:431` の「ケーブルマーカー」にパレット表示名「ケーブル」を併記。
7. 修正後に `docs/PDF/manual.pdf` を再生成（図番号を振り直す場合は本文の図参照も要確認）。

---

## 6. 追記（2026-07-26 二次確認）: 未修正の指摘1件

コミット `3695571`／`df28fe1` で §2 の指摘1〜6はすべて反映済みであることを確認した
（「曲がり角＋」への統一、ボタン名、ブザー12mm/ファン15mmの分離、図9-1重複の解消＝図9-2・図9-3へ振り直し、
ツールバー一覧への「プレビュー」ほか追加、ケーブルの表示名併記）。

その後の追加確認で、**新たに1件の誤りが見つかった**（未修正）。

### 6-1.【要修正】レイヤ管理に「レイヤ追加」機能は無い

| 箇所 | 記載 |
|---|---|
| `manual.md:1557`（図18-1のキャプション） | 「レイヤ管理。各レイヤの表示・ロック・色を一覧で管理し、**新しいレイヤも追加できます**」 |

実装（`showLayerManager`）のダイアログは **表示／ロック／レイヤ名／色の4列テーブルと「適用」ボタンだけ**で、
行を追加するUIが無い。既定レイヤの編集のみが可能。

- 比較: 配線種別管理には「種別追加」ボタンが実在する（`addWireTypeBtn`）ため、
  両者を同じ書き方にすると誤解を招く。
- 本文 `manual.md:1553`「レイヤごとに表示、ロック、色を切り替えます」は正しい。
  **キャプションの「新しいレイヤも追加できます」だけを削除するか、実装側にレイヤ追加を実装する**かの選択になる。

なお §18.2（配線種別管理）の「社内ルールを作る場合は種別を追加し」は実装どおりで問題ない。
