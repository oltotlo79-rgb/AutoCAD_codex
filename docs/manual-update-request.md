# 取扱説明書 修正依頼（codex向け）

- 依頼日: 2026-07-11
- 対象: `docs/manual.md`（修正後、`docs/tools/build-manual-pdf.py` で `docs/PDF/manual.pdf` を再生成してください）
- 依頼理由: 新機能「ページ表示モード（並べて表示）」を実装したため、説明書への反映が必要。
- 実装コミット: このファイルと同じコミットの `index.html` を参照。

## 修正1: §5（ページ）に新しい節「ページ表示モード」を追加

**挿入位置**: 「### 5.2 ページ操作」の直後（既存の 5.3 図枠/表題欄 の前）。節番号は「5.2b」または既存採番に合わせて調整してください。

**追加する内容**（下記をベースに、manual.md の文体・トーンに合わせて調整可）:

```markdown
### 5.2b ページ表示モード（タブ／並べて表示）

左パネル「表示」の **「ページ表示」** で、ページの表示方法を切り替えられます。

- **タブ（1ページずつ）**: 既定。上部タブで切り替えながら1ページずつ表示・編集します。
- **並べて表示（横）**: 全ページを**横一列**に並べ、どのページも直接編集できます。
- **並べて表示（縦）**: 全ページを**縦一列**に並べます。

並べて表示では:

- **クリックしたページが自動的に編集対象**（アクティブ）になります。
  アクティブページは**緑の枠と緑の見出し**で強調され、上部タブの選択も連動します。
- 部品の配置・配線・ダブルクリック編集・右クリックメニューなど、
  すべての操作を**どのページに対しても直接**行えます。
- 各ページ見出しの **◀▶（縦のときは▲▼）** ボタンで、
  ページの**並び順をその場で入れ替え**られます（タブ順・印刷順も連動します）。
- 上部タブをクリックすると、そのページへスクロールして編集対象になります。

ページをまたぐ比較・転記・全体の見通し確認に便利です。
ページ数が多い図面（目安10ページ超）では表示が重くなるため、
通常の作図はタブ表示、確認時に並べて表示、という使い分けをおすすめします。
```

## 修正2: §3.2（左パネル）の「表示」項目一覧に1行追加

「ズーム」「グリッド」「スナップ」などを列挙している箇所に以下を追加:

```markdown
- ページ表示: タブ（1ページずつ）／並べて表示（横・縦）を切り替えます（§5.2b）。
```

## 修正3: §4.2 ページ複製・§5.2 ページ操作からの相互参照（任意）

「前へ/後へ」でページ順を変更する説明がある箇所に、次の一文を追加すると親切です:

```markdown
並べて表示中は、各ページ見出しの◀▶ボタンでも並び順を変更できます（§5.2b）。
```

## 修正4: アプリ内ヘルプとの整合（参考情報・manual.md の修正対象外）

アプリ内ヘルプ（index.html の「ページ操作」節）には今回変更を入れていません。
manual.md 側の記述が確定したら、同じ要約をアプリ内ヘルプへ反映する後続修正を
（私＝Claude側で）行う予定です。文言を合わせたいので、確定版の節タイトルと
1行要約をこのファイルへの返信または commit メッセージで教えてください。

## 動作仕様の正確な参照（執筆時の裏取り用）

- 設定値: `state.settings.multiPageView`（boolean）と `state.settings.multiPageDirection`（"row"＝横・既定 / "column"＝縦）。図面JSONに保存されます。
- 実装関数: `renderCanvasView()` / `activatePage()` / `movePageBySlot()`（index.html内、「ページ表示モード」コメントブロック）。
- 並べて表示中のドラッグ操作は編集中ページのみ再描画する軽量モードで動作します。
- 印刷・PDF・SVG/PNG/DXF出力は表示モードの影響を受けません（従来どおり）。
